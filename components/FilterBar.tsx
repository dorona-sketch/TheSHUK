import React from 'react';

/**
 * @deprecated This component has been replaced by FilterDrawer.tsx and is no longer used in the main application flow.
 * Keeping file structure for reference but component renders nothing or warning.
 */
export const FilterBar: React.FC<any> = () => {
  return null;
};