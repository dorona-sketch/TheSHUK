# Environment variables for The Digital Shuk (example)
# Copy this file to `.env` and fill in real values. Vite only exposes variables prefixed with VITE_.

# Google Gemini API key (do NOT commit the real key)
VITE_GEMINI_API_KEY=

# Optional: API base for your backend proxy that talks to Gemini
VITE_API_BASE_URL=
